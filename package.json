{
  "name": "ig-iap-tunnel-action",
  "version": "1.0.0",
  "description": "GitHub Action to set up an IAP tunnel to a GCP instance group via ig-iap-tunnel",
  "private": true,
  "scripts": {
    "build": "esbuild src/main.ts --bundle --platform=node --target=node24 --outfile=dist/index.js && esbuild src/post.ts --bundle --platform=node --target=node24 --outfile=dist/post/index.js && node scripts/postbuild.js",
    "test": "jest",
    "all": "npm run build && npm test"
  },
  "dependencies": {
    "@actions/cache": "^6.1.0",
    "@actions/core": "^3.0.1",
    "@actions/tool-cache": "^4.0.0"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "@types/node": "^24.13.2",
    "esbuild": "^0.28.1",
    "jest": "^30.4.2",
    "ts-jest": "^29.4.11",
    "typescript": "^6.0.3"
  },
  "overrides": {
    "js-yaml": "^4.2.0"
  }
}
